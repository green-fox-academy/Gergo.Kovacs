#ifndef BIT_ROLL_FILE_IO_BIT_ROLL_H
#define BIT_ROLL_FILE_IO_BIT_ROLL_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int count_characters(char *filename);

int binary_to_decimal(int number);

char *get_conversion(char *filename);

#endif //BIT_ROLL_FILE_IO_BIT_ROLL_H
